# CLAUDE.md

이 파일은 Claude Code(claude.ai/code)가 이 저장소에서 작업할 때 참고하는 가이드입니다.

## 프로젝트 개요

마크다운(.md) 파일을 읽어 정적 블로그 웹사이트로 변환하는 프로젝트입니다.
글쓴이는 마크다운으로 글을 작성하고, 이 프로젝트가 그것을 깔끔한 HTML 블로그 페이지로 빌드해 배포합니다.

## 핵심 원칙

- **프레임워크 없음**: React, Vue, Next.js 등 어떤 프레임워크나 정적 사이트 생성기(Jekyll, Hugo, Astro 등)도 사용하지 않습니다. 순수 HTML, CSS, JavaScript(바닐라 JS)만 사용합니다.
- **의존성 최소화**: 마크다운 파싱 등 꼭 필요한 경우가 아니면 외부 라이브러리를 추가하지 않습니다. 빌드 스크립트에 마크다운 파서가 필요하면 가볍고 의존성이 적은 라이브러리(예: marked)를 Node.js 스크립트에서만 사용하고, 브라우저에 번들러를 붙이지 않습니다.
- **빌드 방식**: 로컬 Node.js 스크립트(`build.js` 등)가 `posts/`의 마크다운 파일을 읽어 정적 HTML 파일로 변환합니다. 결과물은 그대로 어떤 정적 호스팅(GitHub Pages, Netlify 등)에도 올릴 수 있어야 합니다.

## 디자인 요구사항

- **깔끔하고 읽기 좋은 타이포그래피**: 본문 가독성을 최우선으로 합니다. 적절한 줄 간격(line-height 1.6~1.8), 본문 최대 너비(약 680~720px), 넉넉한 여백.
- **다크 모드 지원**: `prefers-color-scheme` 미디어 쿼리로 시스템 설정을 따르되, 사용자가 수동으로 전환할 수 있는 토글 버튼도 제공합니다. 선택한 테마는 `localStorage`에 저장해 새로고침 후에도 유지합니다.
- **모바일 반응형**: 모바일 우선(mobile-first)으로 CSS를 작성하고, `@media` 쿼리로 태블릿/데스크톱 레이아웃을 확장합니다. 폰트 크기, 여백, 네비게이션이 작은 화면에서도 잘 보여야 합니다.
- **불필요한 장식 지양**: 콘텐츠를 방해하지 않는 미니멀한 디자인. 과도한 애니메이션이나 무거운 이미지 지양.

## 예상 프로젝트 구조

```
my-blog/
├── posts/              # 원본 마크다운 글 (예: 2026-09-21-hello-world.md)
├── build.js            # 마크다운 → HTML 변환 스크립트 (Node.js)
├── templates/           # HTML 템플릿 (레이아웃, 포스트, 목록 페이지)
├── public/               # 빌드 결과물 (정적 사이트, 배포 대상)
│   ├── index.html        # 글 목록 페이지
│   ├── posts/             # 변환된 개별 글 HTML
│   └── assets/
│       ├── style.css      # 라이트/다크 모드 포함 스타일시트
│       └── script.js      # 다크 모드 토글 등 클라이언트 스크립트
├── package.json
└── CLAUDE.md
```

## 마크다운 글 형식

각 글은 프론트매터(front matter)를 포함한 마크다운 파일로 작성합니다.

```markdown
---
title: 글 제목
date: 2026-09-21
description: 한두 문장 요약
tags: [태그1, 태그2]
---

본문 내용...
```

## 개발 워크플로

- 새 글 추가: `posts/`에 `.md` 파일 작성 → `node build.js` 실행 → `public/`에 결과물 생성 확인.
- 로컬 미리보기: `public/` 폴더를 간단한 정적 서버(예: `npx serve public` 또는 VS Code Live Server)로 열어 확인합니다.
- 스타일 수정 시 라이트 모드와 다크 모드 양쪽에서 모두 확인합니다.
- 새 기능을 추가하기 전에 "프레임워크 없이 바닐라 HTML/CSS/JS로 구현 가능한가"를 먼저 점검합니다.

## 코딩 컨벤션

- HTML: 시맨틱 태그(`<article>`, `<nav>`, `<header>`, `<footer>` 등) 사용.
- CSS: CSS 커스텀 프로퍼티(변수)로 색상 테마를 관리해 다크 모드 전환을 쉽게 합니다.
- JavaScript: 클래스 없는 순수 함수형 스타일 또는 간단한 모듈 패턴. 불필요한 전역 변수 지양.
- 파일/커밋 메시지: 한국어로 작성해도 무방합니다.
